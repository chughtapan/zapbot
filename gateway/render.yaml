services:
  - type: web
    name: zapbot-gateway
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    envVars:
      - key: GATEWAY_SECRET
        generateValue: true
      - key: PORT
        value: "8080"
