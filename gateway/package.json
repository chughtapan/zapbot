{
  "name": "zapbot-gateway",
  "version": "0.1.0",
  "description": "Lightweight HTTP gateway that routes GitHub webhooks to registered local bridge instances",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "type": "commonjs",
  "devDependencies": {
    "bun-types": "^1.3.12",
    "typescript": "^6.0.2",
    "vitest": "^3.2.4"
  }
}
